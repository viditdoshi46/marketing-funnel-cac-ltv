# Pinned light theme. Soft-gray canvas so the white scorecard panels lift off the
# page with subtle shadows (a flat white-on-white looked cramped).
[theme]
base = "light"
primaryColor = "#2563eb"
backgroundColor = "#f3f5f9"
secondaryBackgroundColor = "#ffffff"
textColor = "#0f172a"
